# Verification packages

## `native-isolate/`

Standalone HAIP Host↔View isolate verification (Quint model + normative review docs).

This package was developed against HAIP PR #5 / head `5edc341`, then cut over to a **native** HAIP UI profile that deliberately does **not** pretend to be MCP Apps or an MCP server.

See `native-isolate/README.md` and `native-isolate/review/haip-agent-ui-profile.md`.
